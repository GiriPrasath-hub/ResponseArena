[project]
name = "responsearena"
version = "1.0.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv==0.1.13",
    "openai>=1.35.0",
    "requests"
]

[project.scripts]
server = "server.app:main"